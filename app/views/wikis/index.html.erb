<h1>All Wikis</h1>

 <% @wikis.each do |x| %>
 <div class="media">
     <div class="media-body">
       <h4 class="media-heading">
         <%= link_to x.title, wiki_path(x.id) %>
       </h4>
     </div>
   </div>
 <% end %>